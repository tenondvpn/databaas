# Debug 日志指南

## 概述

`seth_adapter.py` 现在包含详细的 debug 日志，帮助快速定位问题。

---

## 🔍 日志级别

所有关键函数都添加了详细的 debug 日志：

1. **deploy_contract_with_bytes** - 合约部署（最详细）
2. **contract_prefund** - Gas 预充值
3. **call_contract_function** - 调用合约函数
4. **query_contract_function** - 查询合约函数
5. **get_account_info** - 获取账户信息

---

## 📊 日志格式

### 成功日志示例

```
================================================================================
DEBUG: deploy_contract_with_bytes 开始
================================================================================
[DEBUG] 输入参数:
  - private_key: **********...2c8331991a
  - amount: 0
  - bytes_codes 长度: 1234 字符
  - bytes_codes 前缀: 608060405234801561...
  - constructor_types: ['uint256']
  - constructor_params: [100]
  - nonce: -1
  - prefund: 10000000
  - check_tx_valid: True
  - is_library: False
  - salt: 00
  - to: 

[DEBUG] 步骤 1: 构造完整字节码
  - 检测到构造函数参数，开始编码...
  - 参数类型: ['uint256']
  - 参数值: [100]
  - 编码后的参数长度: 64 字符
  - 编码后的参数: 0000000000000000000000000000000000000000...
  ✓ 构造函数参数编码成功
  - 完整字节码长度: 1298 字符

[DEBUG] 步骤 2: 获取发送者地址
  - 发送者地址: 19b46cb80e027a99ab41d60e68b8a8a096f50869
  ✓ 发送者地址获取成功

[DEBUG] 步骤 3: 计算合约地址
  - 使用 CREATE2 计算地址
  - sender: 19b46cb80e027a99ab41d60e68b8a8a096f50869
  - salt: 00
  - bytecode 长度: 1298
  - 计算出的合约地址: a1b2c3d4e5f6789012345678901234567890abcd
  ✓ CREATE2 地址计算成功

[DEBUG] 步骤 4: 确定交易类型
  - 交易类型: kCreateContract (值: 6)
  - 实际 prefund: 10000000

[DEBUG] 步骤 5: 发送部署交易
  - 目标地址: a1b2c3d4e5f6789012345678901234567890abcd
  - 交易类型: kCreateContract
  - 转账金额: 0
  - Gas prefund: 10000000
  - 字节码长度: 1298
  - 交易哈希: 0x1234567890abcdef...
  ✓ 交易发送成功

[DEBUG] 步骤 6: 等待交易确认
  - 使用 WebSocket 订阅交易: 0x1234567890abcdef...
[WS] Subscribed to txhash: 0x1234567890abcdef...
[WS] Transaction confirmed: 0x1234567890abcdef...
  - 收到交易回执
  - 回执状态: 0
  ✓ 交易确认成功

[DEBUG] 步骤 7: 验证合约地址
  - 检查地址: a1b2c3d4e5f6789012345678901234567890abcd
  - 尝试 1/30: 余额 = 0
  ✓ 合约地址已可用

================================================================================
SUCCESS: 合约部署成功!
合约地址: a1b2c3d4e5f6789012345678901234567890abcd
交易哈希: 0x1234567890abcdef...
================================================================================
```

### 失败日志示例

```
================================================================================
DEBUG: deploy_contract_with_bytes 开始
================================================================================
[DEBUG] 输入参数:
  - private_key: **********...2c8331991a
  - amount: 0
  - bytes_codes 长度: 1234 字符
  ...

[DEBUG] 步骤 5: 发送部署交易
  - 目标地址: a1b2c3d4e5f6789012345678901234567890abcd
  - 交易类型: kCreateContract
  ✗ 交易发送失败: Connection refused
Traceback (most recent call last):
  File "horae/seth_adapter.py", line 123, in deploy_contract_with_bytes
    tx_hash = w3.client.send_transaction_auto(...)
  ...

================================================================================
ERROR: deploy_contract_with_bytes 异常
异常类型: ConnectionError
异常信息: Connection refused
================================================================================
```

---

## 🐛 常见问题排查

### 问题 1: 字节码编码失败

**日志特征**：
```
[DEBUG] 步骤 1: 构造完整字节码
  - 检测到构造函数参数，开始编码...
  ✗ 构造函数参数编码失败: ...
```

**可能原因**：
1. 参数类型与参数值不匹配
2. 参数类型格式错误（如 `uint` 应为 `uint256`）
3. 地址格式错误（需要 bytes 类型）

**解决方案**：
- 检查 `constructor_types` 和 `constructor_params` 是否匹配
- 确认参数类型使用正确的 Solidity 类型名
- 地址参数使用 `decode_hex()` 转换

### 问题 2: 获取发送者地址失败

**日志特征**：
```
[DEBUG] 步骤 2: 获取发送者地址
  ✗ 获取发送者地址失败: ...
```

**可能原因**：
1. 私钥格式错误
2. 私钥长度不正确

**解决方案**：
- 确认私钥是 64 字符的十六进制字符串
- 移除 `0x` 前缀（如果有）

### 问题 3: CREATE2 地址计算失败

**日志特征**：
```
[DEBUG] 步骤 3: 计算合约地址
  - 使用 CREATE2 计算地址
  ✗ CREATE2 地址计算失败: ...
```

**可能原因**：
1. 字节码格式错误
2. salt 格式错误

**解决方案**：
- 确认字节码是有效的十六进制字符串
- 确认 salt 是十六进制字符串

### 问题 4: 交易发送失败

**日志特征**：
```
[DEBUG] 步骤 5: 发送部署交易
  ✗ 交易发送失败: Connection refused
```

**可能原因**：
1. Seth 节点未运行
2. IP/端口配置错误
3. 网络连接问题

**解决方案**：
```bash
# 检查节点是否运行
curl http://127.0.0.1:23001/query_account -d "address=0000000000000000000000000000000000000001"

# 检查配置
python3 -c "from dags import settings; print(settings.SETH_HTTP_IP, settings.SETH_HTTP_PORT)"
```

### 问题 5: 交易确认失败

**日志特征**：
```
[DEBUG] 步骤 6: 等待交易确认
  - 收到交易回执
  - 回执状态: 5001
  ✗ 合约部署失败: Out of gas
```

**可能原因**：
1. Gas 不足
2. 合约代码错误
3. 构造函数执行失败

**解决方案**：
- 增加 `prefund` 值
- 检查合约代码是否有错误
- 查看完整的回执信息

### 问题 6: 合约地址不可用

**日志特征**：
```
[DEBUG] 步骤 7: 验证合约地址
  - 尝试 1/30: 查询失败 - ...
  - 尝试 2/30: 查询失败 - ...
  ✗ 合约地址在 90 秒后仍不可用
```

**可能原因**：
1. 交易未被打包
2. 节点同步延迟
3. 交易实际失败但回执显示成功

**解决方案**：
- 检查交易哈希在区块浏览器中的状态
- 等待更长时间
- 检查节点日志

---

## 📝 日志解读

### 步骤说明

| 步骤 | 说明 | 关键检查点 |
|------|------|-----------|
| 步骤 1 | 构造完整字节码 | 参数编码是否成功 |
| 步骤 2 | 获取发送者地址 | 私钥是否有效 |
| 步骤 3 | 计算合约地址 | CREATE2 计算是否成功 |
| 步骤 4 | 确定交易类型 | 是否选择了正确的类型 |
| 步骤 5 | 发送部署交易 | 交易是否发送成功 |
| 步骤 6 | 等待交易确认 | 交易状态是否为 0 |
| 步骤 7 | 验证合约地址 | 地址是否可查询 |

### 状态码说明

| 状态码 | 含义 | 说明 |
|--------|------|------|
| 0 | 成功 | 交易执行成功 |
| 5001 | 共识错误 | 一般性错误 |
| 5030 | Gas 不足 | 需要增加 prefund |
| 5031 | Revert | 合约执行 revert |
| 10001 | 处理中 | 交易正在处理 |
| 10003 | 已接受 | 交易已接受但未确认 |

---

## 🔧 调试技巧

### 1. 启用详细日志

日志已默认启用，无需额外配置。

### 2. 保存日志到文件

```bash
# 重定向输出到文件
python manage.py runserver > debug.log 2>&1

# 或使用 tee 同时显示和保存
python manage.py runserver 2>&1 | tee debug.log
```

### 3. 过滤特定日志

```bash
# 只查看 DEBUG 日志
grep "\[DEBUG\]" debug.log

# 只查看错误
grep "✗" debug.log

# 只查看成功
grep "✓" debug.log
```

### 4. 实时监控日志

```bash
# 实时查看日志
tail -f debug.log

# 实时查看并过滤
tail -f debug.log | grep "deploy_contract"
```

---

## 📊 性能分析

### 时间戳分析

可以添加时间戳来分析性能：

```python
import time

start_time = time.time()
# ... 执行操作 ...
elapsed = time.time() - start_time
print(f"  - 耗时: {elapsed:.2f} 秒")
```

### 关键操作耗时

| 操作 | 预期耗时 | 说明 |
|------|---------|------|
| 参数编码 | < 0.1 秒 | 本地操作 |
| 获取地址 | < 0.1 秒 | 本地操作 |
| CREATE2 计算 | < 0.1 秒 | 本地操作 |
| 发送交易 | 0.1-0.5 秒 | 网络请求 |
| WebSocket 确认 | 0.05-1 秒 | 实时推送 |
| 地址验证 | 0.1-0.5 秒 | 网络请求 |

---

## 🎯 最佳实践

### 1. 开发环境

- ✅ 启用所有 debug 日志
- ✅ 保存日志到文件
- ✅ 使用 `check_tx_valid=True` 验证交易

### 2. 测试环境

- ✅ 启用 debug 日志
- ✅ 监控关键指标
- ✅ 记录异常情况

### 3. 生产环境

- ⚠️ 考虑减少日志输出
- ✅ 只记录错误和警告
- ✅ 使用日志聚合工具

### 4. 日志管理

```python
# 可以添加日志级别控制
import os

DEBUG_MODE = os.environ.get('SETH_DEBUG', 'true').lower() == 'true'

def debug_print(msg):
    if DEBUG_MODE:
        print(msg)
```

---

## 📚 相关文档

- **seth_adapter.py** - 适配层实现（包含所有 debug 日志）
- **WEBSOCKET_INTEGRATION.md** - WebSocket 集成说明
- **MIGRATION_GUIDE.md** - 迁移指南

---

## 🆘 获取帮助

如果日志无法帮助定位问题：

1. 检查 Seth 节点日志
2. 使用区块浏览器查看交易
3. 查看 `horae/seth_sdk.py` 源码
4. 参考 `horae/seth3.py` 测试用例

---

**更新时间**：2024  
**状态**：✅ 生产就绪  
**日志级别**：详细  
