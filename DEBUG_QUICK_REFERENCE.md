# Debug 快速参考

## 🔍 日志符号说明

| 符号 | 含义 | 示例 |
|------|------|------|
| `[DEBUG]` | 调试信息 | `[DEBUG] 步骤 1: 构造完整字节码` |
| `✓` | 成功 | `✓ 交易发送成功` |
| `✗` | 失败 | `✗ 交易发送失败: Connection refused` |
| `⚠` | 警告 | `⚠ WebSocket 功能不可用` |
| `[WS]` | WebSocket 日志 | `[WS] Subscribed to txhash: 0x...` |

---

## 📊 部署流程检查点

```
步骤 1: 构造字节码
  ├─ 检查参数类型和值是否匹配
  └─ ✓ 编码成功

步骤 2: 获取发送者地址
  ├─ 检查私钥格式
  └─ ✓ 地址获取成功

步骤 3: 计算合约地址
  ├─ 检查 CREATE2 参数
  └─ ✓ 地址计算成功

步骤 4: 确定交易类型
  ├─ kCreateContract (普通合约)
  └─ kCreateLibrary (库合约)

步骤 5: 发送交易
  ├─ 检查节点连接
  └─ ✓ 交易发送成功

步骤 6: 等待确认 (WebSocket)
  ├─ 检查交易状态
  └─ ✓ 交易确认成功

步骤 7: 验证地址
  ├─ 检查地址可用性
  └─ ✓ 合约部署成功
```

---

## 🐛 常见错误速查

### 参数编码错误
```
✗ 构造函数参数编码失败: ...
```
**解决**: 检查参数类型和值是否匹配

### 连接失败
```
✗ 交易发送失败: Connection refused
```
**解决**: 检查节点是否运行，验证 IP/端口

### Gas 不足
```
- 回执状态: 5030
✗ 合约部署失败: Out of gas
```
**解决**: 增加 `prefund` 值

### 地址不可用
```
✗ 合约地址在 90 秒后仍不可用
```
**解决**: 检查交易是否真的成功，查看节点日志

---

## 🔧 快速诊断命令

### 检查节点连接
```bash
curl http://127.0.0.1:23001/query_account \
  -d "address=0000000000000000000000000000000000000001"
```

### 检查配置
```bash
python3 -c "from dags import settings; \
  print(f'HTTP: {settings.SETH_HTTP_IP}:{settings.SETH_HTTP_PORT}'); \
  print(f'WS: {settings.SETH_HTTP_IP}:{settings.SETH_WS_PORT}')"
```

### 测试 WebSocket
```bash
python3 test_websocket.py
```

### 查看日志
```bash
# 只看错误
grep "✗" debug.log

# 只看成功
grep "✓" debug.log

# 看部署流程
grep "deploy_contract" debug.log
```

---

## 📝 日志示例

### 成功部署
```
================================================================================
DEBUG: deploy_contract_with_bytes 开始
================================================================================
[DEBUG] 输入参数:
  - bytes_codes 长度: 1234 字符
  - prefund: 10000000
  ✓ 构造函数参数编码成功
  ✓ 发送者地址获取成功
  ✓ CREATE2 地址计算成功
  ✓ 交易发送成功
[WS] Transaction confirmed: 0x...
  ✓ 交易确认成功
  ✓ 合约地址已可用
================================================================================
SUCCESS: 合约部署成功!
合约地址: a1b2c3d4e5f6...
================================================================================
```

### 失败部署
```
================================================================================
DEBUG: deploy_contract_with_bytes 开始
================================================================================
[DEBUG] 输入参数:
  ...
  ✗ 交易发送失败: Connection refused
================================================================================
ERROR: deploy_contract_with_bytes 异常
异常类型: ConnectionError
异常信息: Connection refused
================================================================================
```

---

## 🎯 状态码速查

| 代码 | 含义 | 处理 |
|------|------|------|
| 0 | 成功 | ✓ 继续 |
| 5030 | Gas 不足 | 增加 prefund |
| 5031 | Revert | 检查合约代码 |
| 10001 | 处理中 | 等待 |
| 10003 | 已接受 | 等待 |

---

## 💡 调试技巧

1. **保存日志**: `python manage.py runserver > debug.log 2>&1`
2. **实时监控**: `tail -f debug.log | grep "DEBUG"`
3. **过滤错误**: `grep "✗" debug.log`
4. **查看步骤**: `grep "步骤" debug.log`

---

## 📞 获取帮助

- **详细指南**: `DEBUG_GUIDE.md`
- **WebSocket**: `WEBSOCKET_INTEGRATION.md`
- **迁移文档**: `MIGRATION_GUIDE.md`
